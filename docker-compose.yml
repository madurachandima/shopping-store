services:
  node-app:
    image: node:20
    container_name: shoping_store
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules  # Prevent overwriting node_modules
    ports:
      - "3000:3000"
    command: ["npm", "start"]
