!imported node-docker-sample is your project name

// if you do some changes to docker file first need to build it
docker build -t node-docker-sample .

// create docker container
docker run -p 3000:3000 node-docker-sample

==============================================

// if you stop the docker contaninrer use this for re run it

# List all containers (including stopped ones)
docker ps -a  

#
docker start -a <container_id_or_name>



docker build -t shopping-store .

docker run -p 3000:3000 shopping-store



docker-compose -f docker-compose.dev.yml down -v  # Stop and remove volumes
docker-compose -f docker-compose.dev.yml build --no-cache  # Rebuild the image
docker-compose -f docker-compose.dev.yml up  # Start fresh


# how to install new node js dependancy inside the docker container 
// shopping_store is my container name

docker exec -it shopping_store sh

npm install --save sequelize

docker-compose up --build # this only run the node container

# to run the full project use
docker-compose -f docker-compose.dev.yml up